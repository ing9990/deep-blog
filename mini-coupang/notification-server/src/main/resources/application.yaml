spring:
  application:
    name: notification-server

server:
  port: 8085

management:
  endpoints:
    web:
      exposure:
        include: health,info

logging:
  level:
    com.deepblog.notification: INFO
