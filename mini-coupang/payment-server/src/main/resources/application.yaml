spring:
  application:
    name: payment-server

server:
  port: 8083

# 결제에 인위적으로 추가하는 지연 (ms). 분산 환경 시뮬레이션 + Saga 보상 시나리오 측정용.
payment:
  simulated-latency-ms: 10000

logging:
  level:
    com.deepblog.payment: INFO
