rootProject.name = "seller-service"

// Multi-module layout (storage:domain, storage:core-db, core:core-api,
// external-modules:*) will be introduced in later steps as the service
// grows. Phase 1 Step 2 ships a single-module Spring Boot Hello.
