# Auto-provisioned Prometheus datasource. Grafana picks this up on first
# start. http://prometheus:9090 resolves via the docker-compose network.

apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: true
